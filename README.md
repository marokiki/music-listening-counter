# Music Listening

ローカルの音楽ファイルを1曲だけ再生するシンプルなWebアプリです。再生完了回数は絶対パスごとに `data/listen-counts.json` へ保存されます。

## 使い方

```bash
npm start
```

ブラウザで `http://localhost:4782` を開き、音声ファイルの絶対パスを入力してください。

## 仕様

- 再生・停止・繰り返し再生
- 曲ごとの再生完了回数を永続化
- カウントクリア
- 途中で大きくシークした再生はカウントしない
